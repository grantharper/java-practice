package com.example;

public class CmdCompilation {

    //to compile this class from the command line run the following command from the root dir of the project
    //javac -d bin src/com/example/CmdCompilation.java
    
    //to run from the command line, run the following command from the root dir of the project
    //java -cp ./bin com.example.CmdCompilation
    
    
    public static void main(String[] args) {
        System.out.println("Successfully compiled and is now running");
    }
    
}
